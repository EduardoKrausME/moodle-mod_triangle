# mod_triangle

Moodle activity module for triangle calculation.

Students may enter known sides and angles. The calculator solves the remaining measurements in the browser, redraws the triangle, updates HTML number input constraints dynamically, and shows derived geometric values and calculation steps.

Supported cases include SSS, SAS, ASA/AAS and SSA (including detection of the ambiguous SSA case).
